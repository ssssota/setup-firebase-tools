{
  "name": "setup-firebase-tools",
  "private": true,
  "version": "0.2.0",
  "scripts": {
    "lint": "rome check src",
    "fmt": "rome format src",
    "check": "rome ci src",
    "build": "esbuild --minify --bundle --platform=node --outfile=dist/index.js src/setup-firebase-tools.ts",
    "prepare": "husky install"
  },
  "dependencies": {
    "@actions/core": "^1.10.0",
    "@actions/http-client": "^2.0.1",
    "@actions/tool-cache": "^2.0.1",
    "uuid": "^9.0.0"
  },
  "devDependencies": {
    "@types/node": "^18.11.16",
    "@types/uuid": "^9.0.0",
    "esbuild": "^0.16.8",
    "husky": "^8.0.0",
    "rome": "^11.0.0"
  },
  "packageManager": "pnpm@7.18.2"
}
